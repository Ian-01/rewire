// ============================================================================
// PLACEHOLDERS TO SWAP — set these two URLs once the backend endpoints exist.
//
// While either URL still starts with "REPLACE_WITH", the app runs in demo mode
// and serves built-in sample data so the dashboard stays demoable.
//
//   DASHBOARD_READ_URL  — GET  {url}?studentId=...   returns the student JSON
//   DASHBOARD_WRITE_URL — POST {url}                 body: { studentId, handoffNote }
// ============================================================================

export const DASHBOARD_READ_URL = "REPLACE_WITH_DASHBOARD_READ_URL";
export const DASHBOARD_WRITE_URL = "REPLACE_WITH_DASHBOARD_WRITE_URL";
